export default function icelandics() {
  let names;
  let names1;
  let names2;
  let rnd;
  let rnd2;
  let rnd;
  let rnd2;
  let names;
  const type = Math.random() < 0.5 ? 0 : 1;
  if (type === 1) {} else {} {
    rnd = Math.floor(Math.random() * names1.length);
    rnd2 = Math.floor(Math.random() * names2.length);
    names = `${names1[rnd]} ${names2[rnd2]}`;
    return names;
  }
}
